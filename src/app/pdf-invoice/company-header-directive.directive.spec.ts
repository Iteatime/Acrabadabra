import { CompanyHeaderDirectiveDirective } from './company-header-directive.directive';

describe('CompanyHeaderDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CompanyHeaderDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
